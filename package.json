{
  "name": "tictactoe",
  "version": "0.1.0",
  "description": "c6u4 nextu web",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && NODE_PATH=./ node dist/src/server",
    "public": "mkdir -p public && cp -r src/client/* public/",
    "build": "npm run compile-server && npm run public",
    "compile-server": "babel src/server/index.js src/server/**/*.js --out-dir dist --presets es2015",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Juank45944/TicTacToe.git"
  },
  "keywords": [
    "nice"
  ],
  "author": "Integ.ro",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Juank45944/TicTacToe/issues"
  },
  "homepage": "https://github.com/Juank45944/TicTacToe#readme",
  "dependencies": {
    "express": "^4.14.0",
    "socket.io": "^1.5.0",
    "socket.io-client": "^1.5.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.16.0"
  }
}
